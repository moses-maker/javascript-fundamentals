// take user inputsusing prompt
let math = Number(prompt("Enter marks for maths:"));
let eng = Number(prompt("Enter marks for english:"));
let kis = Number(prompt("Enter marks for kiswahili:"));

// calculate the totl marks
let total_marks = math + eng + kis;

document.writeln(total_marks);

